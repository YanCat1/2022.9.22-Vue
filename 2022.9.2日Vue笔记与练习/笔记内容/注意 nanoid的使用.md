1. 首先我们需要导入 nanoid这个库

    ```vue
    npm i nanoid
    ```

    导入完毕后 使用es6分别暴露的语法进行nanoid的导入

    在哪个组件中使用 就在哪个组件中导入

    ```
    import {nanoid} from 'nanoid'
    ```

2. 如何使用？

    该文件是一个函数 直接调用即可

    ```
    nano()
    ```

    